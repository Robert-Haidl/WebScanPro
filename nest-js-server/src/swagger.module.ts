// // swagger/swagger.module.ts
// import { Module } from '@nestjs/common';
// import { SwaggerModule, DocumentBuilder } from '@nestjs/swagger';

// @Module({
//   // Add any imports and providers here if necessary
// })
// export class SwaggerModuleConfig {} // Rename to avoid naming conflicts
// export const options = new DocumentBuilder()
//   .setTitle('API Documentation')
//   .setDescription('API description')
//   .setVersion('1.0')
//   .build();

// // You should export the options and use them in your main.ts file
// export const swaggerDocument = SwaggerModule.createDocument(app, options);

// // The following code should be in your main.ts file
// // SwaggerModule.setup('swagger', app, swaggerDocument);
